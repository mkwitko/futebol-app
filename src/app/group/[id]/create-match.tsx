import { ComingSoonScreen } from "@/components/shared/coming-soon";

/** Criar pelada (partida) — chega na próxima task. */
export default function CreateMatchScreen() {
  return <ComingSoonScreen />;
}
